import Registration from "@/components/Registration";

const page = () => {
  return (
    <div className="flex items-center justify-center">
      <Registration />
    </div>
  );
};

export default page;
